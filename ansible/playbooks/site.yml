---
# Master playbook for Ubuntu laptop Jenkins server
# Jenkins, Java 21, Docker already installed and running
# This installs: Trivy, AWS CLI v2, Maven 3.9
# This configures: jenkins docker group, .kube/.aws dirs

- import_playbook: tools.yml
- import_playbook: configure.yml
